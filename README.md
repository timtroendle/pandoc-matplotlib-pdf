This is a test case for using pdf graphs created by matplotlib in pandoc.

Set up your environment using `conda` (or install dependencies manually), and then run through the test using `make test.docx`.
